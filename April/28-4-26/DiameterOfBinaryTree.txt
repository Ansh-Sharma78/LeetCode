/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {
    int count=0;
    public int rec(TreeNode temp,int level){
        if(temp==null){
            return 0;
        }

        int left=rec(temp.left);
        int right=rec(temp.right); 

        return 1 + Math.max(left,right);

    }
    public int diameterOfBinaryTree(TreeNode root) {
        int a= rec(root,0);
        return count;
    }
}